import React, {FC} from 'react';

import {CurrencyModal, CurrencyModalContent} from "../styles/modal";

export const Modal:FC = () => {
    return (
        <CurrencyModal>
                <CurrencyModalContent>
                    fdsfsd
                </CurrencyModalContent>
        </CurrencyModal>
    );
};
